---
title: "eth_blockNumber"
---

POST https://sdo-test.algen.network

Reference: https://algen.docs.buildwithfern.com/asdo-rpc/rpc-reference/rpc-request/eth-block-number

## OpenAPI Specification

```yaml
openapi: 3.1.1
info:
  title: eth_blockNumber
  version: eth_blockNumber
paths:
  '':
    post:
      operationId: eth-block-number
      summary: eth_blockNumber
      tags:
        - []
      parameters: []
      responses:
        '200':
          description: Response with status 200
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EthBlockNumberResult'
components:
  schemas:
    BlockNumber:
      type: string
    BlockNumberTag:
      type: string
      enum:
        - value: earliest
        - value: latest
        - value: pending
    EthBlockNumberResult:
      oneOf:
        - $ref: '#/components/schemas/BlockNumber'
        - $ref: '#/components/schemas/BlockNumberTag'
```

## SDK Code Examples

```python
import requests

url = "https://sdo-test.algen.network/"

response = requests.post(url)

print(response.json())
```

```javascript
const url = 'https://sdo-test.algen.network/';
const options = {method: 'POST'};

try {
  const response = await fetch(url, options);
  const data = await response.json();
  console.log(data);
} catch (error) {
  console.error(error);
}
```

```go
package main

import (
	"fmt"
	"net/http"
	"io"
)

func main() {

	url := "https://sdo-test.algen.network/"

	req, _ := http.NewRequest("POST", url, nil)

	res, _ := http.DefaultClient.Do(req)

	defer res.Body.Close()
	body, _ := io.ReadAll(res.Body)

	fmt.Println(res)
	fmt.Println(string(body))

}
```

```ruby
require 'uri'
require 'net/http'

url = URI("https://sdo-test.algen.network/")

http = Net::HTTP.new(url.host, url.port)
http.use_ssl = true

request = Net::HTTP::Post.new(url)

response = http.request(request)
puts response.read_body
```

```java
HttpResponse<String> response = Unirest.post("https://sdo-test.algen.network/")
  .asString();
```

```php
<?php

$client = new \GuzzleHttp\Client();

$response = $client->request('POST', 'https://sdo-test.algen.network/');

echo $response->getBody();
```

```csharp
var client = new RestClient("https://sdo-test.algen.network/");
var request = new RestRequest(Method.POST);
IRestResponse response = client.Execute(request);
```

```swift
import Foundation

let request = NSMutableURLRequest(url: NSURL(string: "https://sdo-test.algen.network/")! as URL,
                                        cachePolicy: .useProtocolCachePolicy,
                                    timeoutInterval: 10.0)
request.httpMethod = "POST"

let session = URLSession.shared
let dataTask = session.dataTask(with: request as URLRequest, completionHandler: { (data, response, error) -> Void in
  if (error != nil) {
    print(error as Any)
  } else {
    let httpResponse = response as? HTTPURLResponse
    print(httpResponse)
  }
})

dataTask.resume()
```